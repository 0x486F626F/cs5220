# -- Compile settings for the cluster
CPP       = g++ #CC
CPPFLAGS  = -std=c++11 -Wall -O3 -fopenmp -mfma -march=native -ffast-math
CPPFLAGS += -DUSE_BUCKETING
ifdef AVX
CPPFLAGS += -DVEC_BIN 
CPPFLAGS += -DAVX=$(AVX) -mavx2 
endif
#CPPFLAGS += -DSTATS
OPTFLAGS = -O3
LIBS     = -lm
